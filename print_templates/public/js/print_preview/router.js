import DesignerPreview from "./pages/DesignerPreview.vue";

export const routes = [
    {
        name: "auth",
        path: "/authentication",
        component: DesignerPreview,
    }
];

export const AUTH_ROUTES = ["auth"];
